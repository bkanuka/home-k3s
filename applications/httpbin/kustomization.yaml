apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
# NOTE: the shared Gateway is a core service (see /gateway), not part of this app.
- httproute.yaml
# Authentik forward-auth enforcement (in istio-system, scoped to this host only).
- authorizationpolicy.yaml
