apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NVIDIA device plugin — core platform (bootstrapped by install.sh, NOT Argo CD).
# Makes `nvidia.com/gpu` schedulable so GPU workloads (Plex) can transcode.
resources:
- daemonset.yaml
